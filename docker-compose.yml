services:
  nginx:
    build:
      context: .
      dockerfile: nginx/Dockerfile
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-var:/var/lib/letsencrypt
      - certbot-www:/var/www/certbot
    depends_on:
      app:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:80/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  app:
    build: .
    expose:
      - "8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/petroleum_expert.db
    volumes:
      - app-data:/app/data
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    restart: unless-stopped

  # ================================================================
  # Futuro: Descomentar para migrar a PostgreSQL
  # ================================================================
  # 1. Descomentar el servicio postgres abajo
  # 2. Cambiar DATABASE_URL arriba a:
  #    DATABASE_URL=postgresql://petrouser:changeme@postgres:5432/petroleum_expert
  # 3. Agregar psycopg2-binary a requirements.txt
  # 4. docker compose up --build
  # ================================================================

  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: petroleum_expert
  #     POSTGRES_USER: petrouser
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pg-data:/var/lib/postgresql/data
  #   restart: unless-stopped

  # ================================================================
  # Certbot — Let's Encrypt certificate renewal
  # Uncomment to enable automatic HTTPS certificate renewal.
  # First run: bash scripts/init-letsencrypt.sh your-domain.com
  # ================================================================

  # certbot:
  #   image: certbot/certbot
  #   volumes:
  #     - certbot-etc:/etc/letsencrypt
  #     - certbot-var:/var/lib/letsencrypt
  #     - certbot-www:/var/www/certbot
  #   entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
  #   restart: unless-stopped

volumes:
  app-data:
  certbot-etc:
  certbot-var:
  certbot-www:
  # pg-data:
