# ============================================================
# PETROEXPERT v5.0 — Variables de Entorno
# ============================================================
# Copia este archivo como .env y configura los valores necesarios.
# Todas las variables son opcionales — la app funciona sin ninguna.

# --- LLM / IA ---
# ANTHROPIC_API_KEY=tu-api-key-aqui
# GEMINI_API_KEY=tu-gemini-api-key
# GROQ_API_KEY=tu-groq-api-key

# --- Base de datos (opcional — por defecto usa SQLite local) ---
# DATABASE_URL=sqlite:///petroleum_expert.db
# DATABASE_URL=postgresql://user:password@localhost:5432/petroleum_expert

# --- Autenticacion JWT (REQUERIDO en produccion) ---
# ⚠️  Si no configuras JWT_SECRET, la app arranca en modo desarrollo (auth desactivada).
# ⚠️  En produccion (ENVIRONMENT=production), la app NO arranca sin JWT_SECRET.
# Generar con: openssl rand -hex 32
# JWT_SECRET=tu-secreto-jwt-largo-y-aleatorio-minimo-32-caracteres
# JWT_EXPIRE_HOURS=72

# --- Entorno (opcional — fuerza validaciones de seguridad) ---
# ENVIRONMENT=production

# --- API Key Legacy (opcional — sin configurar = solo JWT) ---
# API_KEY=tu-api-key-secreto

# --- CORS Origins (usar IP/dominio del VPS en produccion) ---
# CORS_ORIGINS=http://localhost:5174,https://myapp.com
# Ejemplo produccion: CORS_ORIGINS=http://TU_IP_VPS

# --- Rate Limiting (opcional — valores por defecto razonables) ---
# RATE_LIMIT_AUTH=5/minute       # Login/register (previene brute-force)
# RATE_LIMIT_LLM=10/minute       # Endpoints de IA (previene abuso de costos)
# RATE_LIMIT_CALC=60/minute      # Endpoints de calculo (uso normal)
# RATE_LIMIT_STORAGE=memory://   # Backend de almacenamiento (usar redis:// en produccion)

# --- Registro de Usuarios ---
# ALLOW_REGISTRATION=true         # false para deshabilitar registro publico
# INVITE_CODE=                    # Codigo de invitacion requerido (vacio = no requerido)
